# Copy this file to:
#   keto.env.yml        — local dev (API running outside Docker)
#   keto.env.docker.yml — docker dev (API running inside Docker)
#
# Neither file should be committed — both are gitignored.

dsn: "postgres://replace-user:replace-password@localhost:5432/keto?sslmode=disable"
