# Copy this file to keto.env.yml and fill in the DSN with your credentials.
# Do not commit keto.env.yml — it contains database credentials.

dsn: "postgres://replace-user:replace-password@localhost:5432/keto?sslmode=disable"
